import 'package:flutter/material.dart';

class DetailPaperScreen extends StatelessWidget {
  const DetailPaperScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("asd")
    );
  }
}
